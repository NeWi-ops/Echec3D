#pragma once

// lance les tests statistiques sur le module RandomGenerator
// compare la moyenne théorique de chaque distribution avec la moyenne empirique
// calculée sur N échantillons générés 100000 ça suffit largement et les résultats sont plus que bons
void runStatisticalTests();
